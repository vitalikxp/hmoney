---
created: 2026-05-15
tags: [architecture, data, models]
status: draft
---

# Модели данных

## Firestore коллекции

### `users/{userId}`
```typescript
interface UserProfile {
  displayName?: string;
  currency: string;       // 'RUB'
  createdAt: Timestamp;
  updatedAt: Timestamp;
}
```

### `users/{userId}/categories/{categoryId}`
```typescript
interface Category {
  name: string;
  type: 'income' | 'expense';
  parentId?: string;       // ссылка на родительскую категорию
  icon?: string;           // emoji или icon name
  color?: string;          // hex color
  sortOrder: number;
  createdAt: Timestamp;
  updatedAt: Timestamp;
}
```

### `users/{userId}/accounts/{accountId}`
```typescript
interface Account {
  name: string;            // "Наличные", "Тинькофф", etc.
  type: 'cash' | 'card' | 'savings' | 'investment';
  balance: number;         // текущий баланс в копейках
  currency: string;        // 'RUB'
  icon?: string;
  color?: string;
  sortOrder: number;
  createdAt: Timestamp;
  updatedAt: Timestamp;
}
```

### `users/{userId}/transactions/{transactionId}`
```typescript
interface Transaction {
  date: Timestamp;          // дата транзакции
  amount: number;           // сумма в копейках (отрицательная = расход)
  categoryId: string;       // ссылка на категорию
  accountId: string;        // ссылка на счёт
  description?: string;     // описание
  isRecurring: boolean;
  recurringRule?: {         // если регулярная
    frequency: 'daily' | 'weekly' | 'monthly' | 'yearly';
    interval: number;       // каждые N дней/недель/etc
    endDate?: Timestamp;    // дата окончания
  };
  parentTransactionId?: string; // для регулярных — ссылка на шаблон
  createdAt: Timestamp;
  updatedAt: Timestamp;
}
```

## Индексы Firestore

1. `transactions` — по `date` (desc) + `accountId`
2. `transactions` — по `categoryId` + `date`
3. `transactions` — по `isRecurring` + `date`

## Хранение сумм

Все суммы хранятся в **копейках** (целые числа) чтобы избежать проблем с плавающей точкой.
Отображение: `amount / 100` с двумя знаками после запятой.

## Связанные страницы
- [Функциональные требования](../требования/функциональные-требования.md)
- [UI компоненты](./компоненты.md)
- [Обзор архитектуры](./index.md)
